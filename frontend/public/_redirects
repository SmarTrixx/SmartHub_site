# SPA fallback — ensures client-side routing works
/* /index.html 200

# Optional: Netlify Functions (uncomment if you use functions)
# /api/*    /.netlify/functions/:splat    200

# Example permanent redirect (old path → new path)
# /old-path    /new-path    301

# Optional: Force HTTPS (Netlify also offers this via site settings)
http://*    https://%{host}%{path}    301!